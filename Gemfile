source "https://rubygems.org"

# Keep the local dependency set aligned with the GitHub Pages build image.
# Do not pin a local Ruby release here: GitHub Pages supplies its own runtime.
gem "github-pages", group: :jekyll_plugins

gem "wdm", ">= 0.1.0" if Gem.win_platform?
