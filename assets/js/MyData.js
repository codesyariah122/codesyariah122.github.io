const vm = new Vue({
	el : '#contentApp',
	data:   {
				title1 : obj.title,
				titleHeader : obj.titleHeader,
				blockquoteHeader: obj.blockquoteHeader,
				imageVectorCard : obj.cardVector,
				message: obj.author,
				tagsParallax : obj.tagsParallax,
				imageAuthor2 : obj.authorVector2,
				titleHeaderContact : obj.titleHeader,
		    }
});


